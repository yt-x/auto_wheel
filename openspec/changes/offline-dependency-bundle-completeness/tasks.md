## 1. 变更合并与状态基线

- [x] 1.1 在主变更文档中显式标注“并入 dependency-tree-review-gate 能力”，并补充状态模型总览说明。
- [x] 1.2 在旧变更目录增加“已并入主变更”标记文档，避免继续按旧任务实施。
- [x] 1.3 建立统一状态枚举定义（任务级/依赖级/产物级）及测试夹具，保证后续模块共用。

## 2. 解析与平台覆盖控制

- [x] 2.1 在 `resolver.py` 增加 uv 失败分类器（执行异常/不可满足），并编写单元测试覆盖两类分支与分类结果断言。
- [x] 2.2 重构平台映射逻辑，支持细粒度平台/架构目标；补充参数化测试覆盖 Windows/Linux 不同架构输入。
- [x] 2.3 在解析结果中写入阶段状态与触发原因，补充测试确保 CLI/GUI 使用同一状态语义。

## 3. 依赖树预览与确认闸口

- [x] 3.1 在核心流程接入依赖树预览产物（`dependency-tree.json/.txt` + `coverage-report.md`），并补充文件内容测试。
- [x] 3.2 在 `cli.py/main.py` 新增预览/确认参数（如 `--plan-only`、`--approve-tree`），编写参数互斥与兼容性测试。
- [x] 3.3 在 `gui/forms.py/workers.py/main_window.py` 增加高级模式确认闸口，编写行为测试验证“未确认阻断、确认放行”。

## 4. 老旧包弹性采集与防污染回退

- [x] 4.1 在 `downloader.py` 实现源码弹性采集分支（优先直连采集，失败回退 pip），并编写单元测试覆盖成功与失败路径。
- [x] 4.2 增加回退一致性约束，防止主机平台 wheel 污染目标结果，并编写回归测试验证跨平台输入时的产物标记。
- [x] 4.3 为老旧样本（如 `thrift/mysqlclient/tensorflow1.15`）补充实验型回归测试夹具，验证阶段化错误摘要与建议动作。

## 5. 可安装性预演与状态落盘

- [x] 5.1 在 `requirements_generator.py` 或独立模块实现离线可安装性预演执行器，编写测试覆盖预演通过与失败报告生成。
- [x] 5.2 新增 `installability-report.md` 输出，验证报告包含时间、目标参数、失败清单、建议动作和状态快照。
- [x] 5.3 将任务级/依赖级/产物级状态回写主流程结果结构，编写端到端测试确保失败时返回非零状态。

## 6. 文档与验证闭环

- [x] 6.1 更新 `README.md`、`QUICKSTART.md`、`docs/TECHNICAL_OVERVIEW.md`，补充“基础模式 vs 高级模式”与状态说明。
- [x] 6.2 新增 `evidence/` 验证模板（输入、目标环境、覆盖状态、预演结果、状态迁移摘要），并在实现完成后填充真实样本。
- [x] 6.3 组织一次综合回归（Windows 本机 + Linux 目标），输出测试摘要并校验私网交付证据文件齐全性。
