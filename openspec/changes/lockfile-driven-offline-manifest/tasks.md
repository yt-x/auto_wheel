## 1. 生成器能力重构

- [x] 1.1 为 `RequirementsGenerator.generate()` 增加 `resolved_requirements` 可选入参，并在有值时按锁定列表生成 `requirements-offline.txt`（测试：新增锁定输入生成用例）。
- [x] 1.2 保留目录扫描逻辑作为兜底路径，并输出 `manifest_mode`（`lock`/`non_lock`）到生成结果摘要（测试：新增无锁定输入兼容用例）。
- [x] 1.3 新增“锁定与产物对账”输出结构（缺失项、仅源码项、额外项）（测试：构造目录污染样例验证对账字段）。

## 2. CLI/GUI 链路对齐

- [x] 2.1 在 `main.py` 中将 `resolved_packages` 透传给生成器，统一锁定模式生成路径（测试：CLI 集成测试校验离线清单与锁定列表一致）。
- [x] 2.2 在 `gui/workers.py` 中同步透传并输出一致模式日志（测试：GUI Worker 单测校验调用参数与模式日志）。
- [x] 2.3 保持现有下载与回退分支不变，仅调整清单来源与报告输出（测试：fallback 回归测试确保旧行为不退化）。

## 3. 回归防护与文档更新

- [x] 3.1 增加“历史残留 wheel 不得污染锁定模式清单”的回归测试（测试：同包多版本目录样本）。
- [x] 3.2 增加“非锁定模式仍可生成但有风险标识”的回归测试（测试：uv 不可用场景）。
- [x] 3.3 更新 README/QUICKSTART 对锁定模式与非锁定模式的说明及排障建议（测试：文档检查与示例命令可执行性人工验证）。
