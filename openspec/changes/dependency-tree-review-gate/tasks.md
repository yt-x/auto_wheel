## 1. 预览与覆盖数据模型

- [ ] 1.1 在核心层新增依赖树/覆盖报告数据结构与序列化输出（`json/txt/md`），并补充单元测试验证字段完整性与文件落盘。
- [ ] 1.2 扩展 `requirements_generator` 支持生成 `dependency-tree.txt` 与 `coverage-report.md`，并补充测试覆盖空结果、部分覆盖与未知状态。

## 2. 解析器与下载器边界收敛

- [ ] 2.1 重构 `resolver` 平台映射逻辑，支持细粒度平台/架构目标；补充参数化测试覆盖 Windows/Linux 不同架构输入。
- [ ] 2.2 重构 `downloader` 回退策略为“仅补拉源码包或显式失败”，并补充测试防止主机平台 wheel 污染。
- [ ] 2.3 增强阶段化错误归因（`wheel_only/source_fallback`）写入覆盖报告，补充网络超时与 pip 不可用测试。

## 3. CLI 流程与高级闸口

- [ ] 3.1 在 `cli.py` 增加预览/确认相关参数（如 `--plan-only`、`--approve-tree`），并补充参数互斥与缺参测试。
- [ ] 3.2 在 `main.py` 接入“两阶段执行模型”，实现高级模式“未确认不下载”约束，并补充端到端流程测试（兼容旧参数）。
- [ ] 3.3 优化 CLI 输出文案，展示覆盖统计与确认状态，并补充快照或关键文案断言测试。

## 4. GUI 预览与确认交互

- [ ] 4.1 在 `gui/forms.py` 增加高级模式与确认字段，保持默认隐藏；补充表单状态转换测试。
- [ ] 4.2 在 `gui/workers.py` 接入预览阶段与确认闸口，确保与 CLI 语义一致；补充 worker 行为测试。
- [ ] 4.3 在 `gui/main_window.py` 增加依赖树摘要展示与确认操作，并补充关键交互测试（未确认阻断、确认后放行）。

## 5. 回归验证与文档

- [ ] 5.1 新增跨平台样例回归（含无 wheel 包）验证覆盖报告准确性，并记录失败分层线索。
- [ ] 5.2 更新 `README.md` 与 `docs/TECHNICAL_OVERVIEW.md` 的高级流程说明，补充“默认兼容 vs 高级确认”对比示例。
- [ ] 5.3 增加 `evidence/` 验证记录模板（输入、平台、覆盖结果、确认摘要），并在变更完成时填充一份示例记录。
