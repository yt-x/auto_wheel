# 源码包依赖下载失败修复计划（auto-wheel）

## 1. 背景与问题定义

### 1.1 现象
- 在下载依赖时，如果某些三方包仅提供源码分发（sdist，如 `.tar.gz/.zip`）而没有目标平台 wheel，当前流程会直接失败。
- 典型场景：`apache-iotdb` 相关依赖链中出现仅源码包时，下载阶段终止，后续离线安装流程无法继续。

### 1.2 根因分析
- 当前默认参数是 `--only-binary :all:`，即“只允许下载 wheel”。
- 当依赖不存在可用 wheel 时，`pip download` 会报错退出（如 `No matching distribution found` / `Could not find a version`）。
- 现有代码虽已具备源码包分流能力（`sources/`、`sources-offline.txt`、`SOURCE_INSTALL_GUIDE.md`），但该能力依赖“下载阶段成功结束”；在严格 wheel-only 失败时无法触发。

### 1.3 目标
- 保留“wheel 优先”策略，提高离线安装稳定性。
- 当仅因“无 wheel”失败时，自动回退一次允许下载源码包，确保离线交付产物可生成。
- 安装脚本不自动安装源码包，只输出明确指引，避免污染环境与半安装状态。

## 2. 方案原则与边界

### 2.1 固定原则
- 默认继续优先 wheel。
- 仅在识别到“无 wheel 可用”错误时触发回退，不对网络/权限等错误盲目回退。
- 继续强制在已激活虚拟环境中安装。
- 安装脚本检测到源码包清单时直接停止，并要求先按指南处理。

### 2.2 明确不做
- 不自动执行 `setup.py install`。
- 不在本次新增 GUI 配置项或改变现有 CLI 参数结构。

## 3. 实施步骤

### 步骤 1：下载器增加“无 wheel 失败自动回退”
- 在下载执行路径中加入两阶段机制：
1. 第一阶段：按原命令执行（保留 `--only-binary`）。
2. 失败判定：从 stderr/stdout 识别“无 wheel/无可用发行版”特征。
3. 第二阶段：移除 `--only-binary` 后重试下载（允许 sdist）。
- 仅在上述特征命中时触发第二阶段，其他错误类型保持原样返回。

### 步骤 2：结果结构与日志增强
- 在下载结果中增加字段：
1. `used_source_fallback`：是否触发源码包回退。
2. `fallback_reason`：触发回退的识别原因（摘要）。
- 在 verbose/失败日志中输出回退前后关键信息，便于排障。

### 步骤 3：CLI 与 GUI 提示对齐
- CLI 主流程与 GUI worker 都基于新字段输出一致提示：
1. 若触发回退并成功：提示“已下载源码包，请先处理 `SOURCE_INSTALL_GUIDE.md`”。
2. 若回退后仍失败：输出两阶段失败线索，便于定位。
- 汇总区继续展示 `wheels` 与 `source packages` 数量。

### 步骤 4：保持安装阶段策略
- 保持 `install.sh/install.bat` 当前行为不变：
1. 未激活虚拟环境时拒绝安装。
2. 检测到 `sources-offline.txt` 非空时停止并提示先处理源码包。

### 步骤 5：测试与验收
- 新增/完善测试覆盖：
1. wheel-only 成功：不触发回退。
2. wheel-only 因无 wheel 失败：触发回退并成功。
3. 非无 wheel 错误（网络/权限）失败：不触发回退。
4. 回退后仍失败：错误信息完整可追踪。
- 人工验收（关键）：
1. 使用 apache-iotdb 相关依赖输入执行下载。
2. 验证存在 `sources/`、`sources-offline.txt`、`SOURCE_INSTALL_GUIDE.md`。
3. 验证安装脚本先提示处理源码包并停止。
4. 人工处理源码包后可继续安装 `requirements-offline.txt` 中 wheel 依赖。

### 步骤 6：文档同步
- 更新 `README.md`、`QUICKSTART.md`、`docs/TECHNICAL_OVERVIEW.md`：
1. 增加“默认 wheel 优先 + 自动源码回退”说明。
2. 强调“脚本不自动安装源码包，仅给处理指引”。
3. 保持示例命令统一使用 `python -m pip`。

## 4. 风险与应对

### 风险 1：错误识别过宽，导致误回退
- 应对：仅匹配稳定的“无可用发行版”关键模式；测试覆盖误判场景。

### 风险 2：触发回退后下载耗时增加
- 应对：仅在首轮失败且命中特征时回退，避免默认双下载。

### 风险 3：用户误以为脚本会自动处理源码包
- 应对：CLI/GUI/文档统一强调“先处理源码包，再安装 wheel 依赖”。

## 5. 交付物清单
- 代码：
1. 下载器回退逻辑与结果字段。
2. CLI/GUI 输出提示对齐。
- 文档：
1. README / QUICKSTART / TECHNICAL_OVERVIEW 同步更新。
- 验证：
1. 单元测试与关键场景人工验证记录。

## 6. 验收标准（Definition of Done）
- 遇到仅有源码包的依赖时，下载流程不再直接失败退出，而是自动回退后成功产出离线目录。
- 目录内可见源码包清单与指南，安装脚本在源码包未处理前会明确阻断并给出指引。
- 文档与实际行为一致，无旧逻辑冲突描述。
